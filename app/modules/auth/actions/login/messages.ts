import { createI18nMessage } from '@/app/shared';

export const INVALID_CREDENTIAL_MESSAGE = createI18nMessage('auth.login.message.error.credential');
export const DEFAULT_LOGIN_ERROR_MESSAGE = createI18nMessage('auth.login.message.error.default');
export const PASSWORD_RULE_MESSAGE = createI18nMessage('auth.login.message.error.password');